/**
 * Constants available both in Javascript via `import` and SCSS via globally defined variables.
 *
 * @type {Object}
 */
module.exports = {
  /**
   * Number of cells per board row and column.
   *
   * @type {Number}
   */
  BOARD_SIZE: 8,

  /**
   * Header height.
   *
   * @type {String}
   */
  HEADER_HEIGHT: '2.5em',
};
